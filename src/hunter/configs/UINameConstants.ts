/**
 * 各个UI的名字表,UI的名字必须是UI的类标识符，显示UI时使用或隐藏UI时需要使用
*/
export default class UINameConstants {
    // static makeUIName = AssetsUtils.makeUIName;

    /**
     * UI包定义
     */
    static FUI_PACKAGE_NAME_LOADING = "loading";
    static FUI_PACKAGE_NAME_COMMON = "Common";

    /**
     * UI名定义
     */
    // static FUI_NAME_FOOTBALL_LOADING_VIEW = makeUIName(FUI_PACKAGE_NAME_FOOTBALL, footBall.FUIFootballLoadingView.URL);

}